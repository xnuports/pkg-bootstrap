# Repository meta configuration
# This file is served at the repository root
# Format: pkg+https://xnuports.github.io/xnuports/packages/macOS-${arch}/latest

# Repository information
name: "xnuports"
url: "pkg+https://xnuports.github.io/xnuports/packages"
version: "1"

# Architecture variants
architectures: arm64 x86_64

# Paths (relative to this file)
packagesite: "."
manifests: "manifests"
packages: "packages"

# Signature (disabled until keys are set up)
signature_type: none
